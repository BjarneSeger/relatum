apiVersion: v1
kind: Service
metadata:
  name: {{ include "relatum-web.fullname" . }}
  labels:
    {{- include "relatum-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "relatum-web.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
